import React from 'react';
import '../styles/components/Button.css';

const LoadButton = ({title}) => {
    return (
        <div className="Button">
            {title}
        </div>
    );
}

export default LoadButton;
